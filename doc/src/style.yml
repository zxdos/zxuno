extends: default

header:
  height: 15mm
  font-family: M+ 1mn
  recto:
    right:
      content: "{document-title} : {section-or-chapter-title}"
  verso:
    left:
      content: "{document-title} : {section-or-chapter-title}"

role:
  tiny:
    font-size: 6
